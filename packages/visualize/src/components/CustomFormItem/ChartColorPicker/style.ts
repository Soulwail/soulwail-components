import { createStyles } from 'antd-style';

export default createStyles(({ css }) => {
    return {
        'custom-panel': css`
            .site-color-picker-clear {
                margin-inline-start: unset !important;
            }
        `,
    };
});
